node_modules/
.wrangler/
.dev.vars
dist/
worker-configuration.d.ts

# Generated by build_corpus.py. Safe to commit if you'd rather not require
# torch on every machine that deploys - it's ~250KB of JSON.
# src/corpus.json
